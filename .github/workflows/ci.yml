name: CI - Modular Personal Finance Tracker

on:
  push:
  pull_request:

jobs:
  test-modules:
    name: Test ${{ matrix.module }} (Node ${{ matrix.node }})
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        node: [18, 20]
        module: [dashboard, expenses, income]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}

      - name: Install dependencies
        working-directory: personal-finance-tracker-1/${{ matrix.module }}
        run: npm install

      - name: Run tests
        working-directory: personal-finance-tracker-1/${{ matrix.module }}
        run: npm test

  build-app:
    name: Build Full Application
    runs-on: ubuntu-latest
    needs: test-modules

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install root dependencies
        working-directory: personal-finance-tracker-1
        run: npm install

      - name: Build all modules
        working-directory: personal-finance-tracker-1
        run: npm run build
